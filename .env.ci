# CI/CD Environment Configuration
# This file is used by GitHub Actions for CI/CD pipeline

# Airflow Configuration
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# Database Configuration (CI specific)
POSTGRES_HOST=genetl-postgres
POSTGRES_PORT=5432
POSTGRES_DB=genetl_warehouse
POSTGRES_USER=genetl
POSTGRES_PASSWORD=test_password_ci

# Redis Configuration
REDIS_HOST=genetl-redis
REDIS_PORT=6379

# Airflow Web UI Admin User
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin_change_in_prod

# Security - Use test key for CI
AIRFLOW__CORE__FERNET_KEY=zP0KVBMjQ0zIbD3fkGjHxhjNZNUHq2okB2Lp3fkF1RY=