# GenETL Dependencies
# Astro runtime already includes Apache Airflow and core providers
# Only add additional providers if needed
# apache-airflow-providers-postgres  # Already included in Astro runtime
# apache-airflow-providers-redis     # Already included in Astro runtime

# Data processing and ETL dependencies
pandas
numpy
requests
psycopg2-binary
SQLAlchemy>=1.4,<2.0
great-expectations
redis>=4.0.0

# AI and ML dependencies
scikit-learn
openai

# Constraints to prevent build issues in CI
# Prevent google-re2 from being installed if it has build issues
# Use binary distributions only

# Development and testing dependencies
pytest
pytest-cov
black
isort
flake8
mypy
safety
bandit