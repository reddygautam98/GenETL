# Constraints file to prevent build issues in CI/CD
# This file specifies version constraints for packages that should only use binary wheels

# Prevent google-re2 from being built from source
# Use only binary wheels if available
google-re2==1.1.20250805

# Other packages that commonly have build issues
# Ensure we use compatible versions with binary wheels available
pandas>=1.3.0,<3.0.0
numpy>=1.21.0,<3.0.0
psycopg2-binary